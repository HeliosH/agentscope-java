# Copy to .env.clawsentry or export these variables in the shell before compose.
# A non-empty token is required when this gateway is reachable outside localhost.
CS_AUTH_TOKEN=CHANGE_ME_TO_A_RANDOM_TOKEN
CLAWSENTRY_HTTP_PORT=18081
# Optional: override the pinned source commit for a reviewed upgrade.
# CLAWSENTRY_REF=2a3a826bb731cb18db88f84be9a6dd3974a321f9
